<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="top.itfinally.security.repository.mapper.RolePermissionMapper">
    <resultMap id="rolePermissionEntity" type="top.itfinally.security.repository.po.RolePermissionEntity"
               extends="top.itfinally.core.repository.mapper.BaseMapper.baseEntity">
        <association property="role" column="role" select="top.itfinally.security.repository.mapper.RoleMapper.query"/>
        <association property="permission" column="permission"
                     select="top.itfinally.security.repository.mapper.PermissionMapper.query"/>
    </resultMap>

    <cache-ref namespace="top.itfinally.core.repository.mapper.BaseMapper"/>

    <select id="query" resultMap="rolePermissionEntity">
        select * from v1_security_role_permission where id = #{id}
    </select>

    <select id="queryAll" resultMap="rolePermissionEntity">
        select * from v1_security_role_permission where 1 = 1
        <if test="status != -1001">
            and status = #{status}
        </if>
        <if test="beginRow != -1000 and row != -1000">
            limit #{beginRow}, #{row}
        </if>
    </select>

    <select id="queryBySpecificId" resultMap="rolePermissionEntity">
        select * from v1_security_role_permission where id in
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
        <if test="status != -1001">
            and status = #{status}
        </if>
        <if test="beginRow != -1000 and row != -1000">
            limit #{beginRow}, #{row}
        </if>
    </select>

    <insert id="save">
        INSERT INTO v1_security_role_permission (
            id, create_time, update_time, delete_time, status, role_id, permission_id

        ) VALUES (
            #{id}, #{createTime}, #{updateTime}, #{deleteTime}, #{status}, #{role.id}, #{permission.id}
        );
    </insert>

    <insert id="saveAll">
        INSERT INTO v1_security_role_permission (
        id, create_time, update_time, delete_time, status, role_id, permission_id

        ) VALUES
        <foreach collection="entities" item="entity" open="(" separator="), (" close=")">
            #{entity.id},
            #{entity.createTime},
            #{entity.updateTime},
            #{entity.deleteTime},
            #{entity.status},
            #{entity.role.id},
            #{entity.permission.id}
        </foreach>
    </insert>

    <update id="update">
        UPDATE v1_security_role_permission
        SET
            update_time   = #{updateTime},
            delete_time   = #{deleteTime},
            status        = #{status},
            role_id       = #{role.id},
            permission_id = #{permission.id}
        WHERE id = #{id}
    </update>

    <update id="specificUpdate">
        update v1_security_role_permission set update_time = #{updateTime}
        <foreach collection="fieldAndValues.entrySet()" index="key" item="value" open="," separator=",">
            ${key} = #{value}
        </foreach>
        where id = #{id}
    </update>

    <update id="remove">
        UPDATE v1_security_role_permission
        SET
        update_time = #{deleteTime},
        delete_time = #{deleteTime},
        status      = -1
        WHERE id = #{id}
    </update>

    <update id="removeAll">
        update v1_security_role_permission set
        update_time = #{deleteTime},
        delete_time = #{deleteTime},
        status = -1
        where id in
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </update>

    <delete id="physicalDelete">
        DELETE FROM v1_security_role_permission
        WHERE id = #{id}
    </delete>

    <delete id="physicalDeleteAll">
        delete from v1_security_role_permission where id in
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

    <select id="queryByRoleId" resultMap="rolePermissionEntity">
        SELECT *
        FROM v1_security_role_permission
        WHERE role_id = #{roleId}
    </select>

    <select id="hasPermission" resultType="boolean">
        SELECT 1
        FROM v1_security_role_permission
        WHERE permission_id = #{permissionId}
        <if test="status != -1001">
            and status = #{status}
        </if>
        LIMIT 1
    </select>

    <select id="hasAllPermission" resultType="boolean">
        SELECT 1
        FROM v1_security_role_permission
        WHERE permission_id IN
        <foreach collection="permissionIds" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
        <if test="status != -1001">
            and status = #{status}
        </if>
        LIMIT 1
    </select>

    <select id="hasRole" resultType="boolean">
        SELECT 1
        FROM v1_security_role_permission
        WHERE role_id = #{roleId}
        <if test="status != -1001">
            and status = #{status}
        </if>
        LIMIT 1
    </select>

    <select id="hasAllRole" resultType="boolean">
        SELECT 1
        FROM v1_security_role_permission
        WHERE role_id IN
        <foreach collection="roleIds" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
        <if test="status != -1001">
            and status = #{status}
        </if>
        LIMIT 1
    </select>
</mapper>