<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="top.itfinally.admin.repository.mapper.MenuItemMapper">
    <resultMap id="menuItemEntity" type="top.itfinally.admin.repository.po.MenuItemEntity"
               extends="top.itfinally.core.repository.mapper.BaseMapper.baseEntity">
        <result property="name" column="name" javaType="java.lang.String"/>
        <result property="isRoot" column="is_root" javaType="boolean"/>
        <result property="isLeaf" column="is_leaf" javaType="boolean"/>
    </resultMap>

    <cache-ref namespace="top.itfinally.core.repository.mapper.BaseMapper"/>

    <select id="query" resultMap="menuItemEntity">
        SELECT *
        FROM v1_menu_item
        WHERE id = #{id}
    </select>

    <select id="queryAll" resultMap="menuItemEntity">
        select * from v1_menu_item where 1 = 1
        <if test="status != -1001">
            and status = #{status}
        </if>
        <if test="beginRow != -1000 and row != -1000">
            limit #{beginRow}, #{row}
        </if>
    </select>

    <select id="queryBySpecificId" resultMap="menuItemEntity">
        select * from v1_menu_item where id in
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
        <if test="status != -1001">
            and status = #{status}
        </if>
        <if test="beginRow != -1000 and row != -1000">
            limit #{beginRow}, #{row}
        </if>
    </select>

    <insert id="save">
        INSERT INTO v1_menu_item (
            name, is_root, is_leaf, id, status, create_time,
            update_time, delete_time

        ) VALUES (
            #{name}, #{isRoot}, #{isLeaf}, #{id}, #{status}, #{createTime},
            #{updateTime}, #{deleteTime}
        );
    </insert>

    <insert id="saveAll">
        INSERT INTO v1_menu_item (
        name, is_root, is_leaf, id, status, create_time,
        update_time, delete_time

        ) VALUES
        <foreach collection="entities" item="entity" open="(" separator="), (" close=")">
            #{entity.name}, #{entity.isRoot}, #{entity.isLeaf}, #{entity.id}, #{entity.status}, #{entity.createTime},
            #{entity.updateTime}, #{entity.deleteTime}
        </foreach>
    </insert>

    <update id="update">
        UPDATE v1_menu_item
        SET
            name        = #{name},
            is_root     = #{isRoot},
            is_leaf     = #{isLeaf},
            status      = #{status},
            update_time = #{updateTime},
            delete_time = #{deleteTime}

        WHERE id = #{id}
    </update>

    <update id="specificUpdate">
        update v1_menu_item set update_time = #{updateTime}
        <foreach collection="fieldAndValues.entrySet()" index="key" item="value" open="," separator=",">
            ${key} = #{value}
        </foreach>
        where id = #{id}
    </update>

    <update id="remove">
        UPDATE v1_menu_item
        SET
            update_time = #{deleteTime},
            delete_time = #{deleteTime},
            status      = -1
        WHERE id = #{id}
    </update>

    <update id="removeAll">
        update v1_menu_item set
        update_time = #{deleteTime},
        delete_time = #{deleteTime},
        status = -1
        where id in
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </update>

    <delete id="physicalDelete">
        DELETE FROM v1_menu_item
        WHERE id = #{id}
    </delete>

    <delete id="physicalDeleteAll">
        delete from v1_menu_item where id in
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

    <select id="queryRootItem" resultMap="menuItemEntity">
        SELECT *
        FROM v1_menu_item
        WHERE is_root = TRUE
    </select>

    <select id="queryByName" resultMap="menuItemEntity">
        SELECT *
        FROM v1_menu_item
        WHERE name = #{name}
        LIMIT 1;
    </select>
</mapper>