<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="top.itfinally.admin.repository.mapper.RoleMenuItemMapper">
    <resultMap id="roleMenuItemEntity" type="top.itfinally.admin.repository.po.RoleMenuItemEntity"
               extends="top.itfinally.core.repository.mapper.BaseMapper.baseEntity">
        <association property="role" column="role_id"
                     select="top.itfinally.security.repository.mapper.RoleMapper.query"/>
        <association property="menuItem" column="menu_item_id"
                     select="top.itfinally.admin.repository.mapper.MenuItemMapper.query"/>
    </resultMap>

    <cache-ref namespace="top.itfinally.core.repository.mapper.BaseMapper"/>

    <select id="query" resultMap="roleMenuItemEntity">
        SELECT *
        FROM v1_menu_scope
        WHERE id = #{id}
    </select>

    <select id="queryAll" resultMap="roleMenuItemEntity">
        select * from v1_menu_scope
        <where>
            <if test="status != -1001">
                and status = #{status}
            </if>
            <if test="beginRow != -1000 and row != -1000">
                limit #{beginRow}, #{row}
            </if>
        </where>
    </select>

    <select id="queryBySpecificId" resultMap="roleMenuItemEntity">
        select * from v1_menu_scope where id in
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
        <if test="status != -1001">
            and status = #{status}
        </if>
        <if test="beginRow != -1000 and row != -1000">
            limit #{beginRow}, #{row}
        </if>
    </select>

    <insert id="save">
        INSERT INTO v1_menu_scope (
            role_id, menu_item_id, id, status, create_time, update_time,
            delete_time

        ) VALUES (
            #{role.id}, #{menuItem.id}, #{id}, #{status}, #{createTime}, #{updateTime},
            #{deleteTime}
        );
    </insert>

    <insert id="saveAll">
        INSERT INTO v1_menu_scope (
        role_id, menu_item_id, id, status, create_time, update_time,
        delete_time

        ) VALUES
        <foreach collection="entities" item="entity" open="(" separator="), (" close=")">
            #{entity.role.id}, #{entity.menuItem.id}, #{entity.id}, #{entity.status}, #{entity.createTime},
            #{entity.updateTime},
            #{entity.deleteTime}
        </foreach>
    </insert>

    <update id="update">
        UPDATE v1_menu_scope
        SET
            role_id      = #{role.id},
            menu_item_id = #{menuItem.id},
            status       = #{status},
            update_time  = #{updateTime},
            delete_time  = #{deleteTime}

        WHERE id = #{id}
    </update>

    <update id="specificUpdate">
        update v1_menu_scope set update_time = #{updateTime}
        <foreach collection="fieldAndValues.entrySet()" index="key" item="value" open="," separator=",">
            ${key} = #{value}
        </foreach>
        where id = #{id}
    </update>

    <update id="remove">
        UPDATE v1_menu_scope
        SET
            update_time = #{deleteTime},
            delete_time = #{deleteTime},
            status      = -1
        WHERE id = #{id}
    </update>

    <update id="removeAll">
        update v1_menu_scope set
        update_time = #{deleteTime},
        delete_time = #{deleteTime},
        status = -1
        where id in
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </update>

    <delete id="physicalDelete">
        DELETE FROM v1_menu_scope
        WHERE id = #{id}
    </delete>

    <delete id="physicalDeleteAll">
        delete from v1_menu_scope where id in
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

    <select id="queryByRoleId" resultMap="roleMenuItemEntity">
        SELECT *
        FROM v1_menu_scope
        WHERE role_id = #{roleId}
        <if test="status != -1001">
            and status = #{status}
        </if>
    </select>

    <select id="queryByMenuId" resultMap="roleMenuItemEntity">
        SELECT *
        FROM v1_menu_scope
        WHERE menu_item_id = #{menuId}
        <if test="status != -1001">
            and status = #{status}
        </if>
    </select>

    <select id="queryByMultiRoleIds" resultMap="roleMenuItemEntity">
        SELECT *
        FROM v1_menu_scope
        WHERE role_id IN
        <foreach collection="roleIds" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
        <if test="status != -1001">
            and status = #{status}
        </if>
    </select>

    <select id="queryRoleMenuItemChain" resultMap="roleMenuItemEntity">
        SELECT *
        FROM v1_menu_scope
        WHERE role_id = #{roleId}
        <if test="status != -1001">
            and status = #{status}
        </if>
        AND menu_item_id IN
        <foreach collection="menuItemIds" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </select>
</mapper>