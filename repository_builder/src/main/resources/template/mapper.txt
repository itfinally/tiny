package ${package}.mapper${util.getOffset( ${table.thisEntity.offset} )};

import org.apache.ibatis.annotations.Mapper;
import org.springframework.stereotype.Component;
import ${package}.mapper${util.getOffset( ${table.extendEntity.offset} )}.${table.extendEntity.baseName}Mapper;
import ${package}.po${util.getOffset( ${table.thisEntity.offset} )}.${table.thisEntity.simpleName};

@Mapper
@Component
public interface ${table.thisEntity.baseName}Mapper extends ${table.extendEntity.baseName}Mapper<${table.thisEntity.simpleName}> {
}